## Test Configuration for StudyConnect Backend (runtime profile "test")
#
# This profile is intended for local development and automated tests where
# an in-memory H2 database is sufficient. It overrides the default
# PostgreSQL configuration from application.properties.

# Server Configuration for Tests
server.port=8080
server.servlet.context-path=/api

# Database Configuration for Tests (H2 in-memory database)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration for Tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Security Configuration for Tests
spring.security.user.name=testuser
spring.security.user.password=testpass
spring.security.user.roles=USER

# JWT Configuration for Tests
jwt.secret=test-jwt-secret-key-for-testing-only
jwt.expiration=3600000

# Logging Configuration for Tests
logging.level.com.studyconnect.backend=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# Disable security auto-configuration for tests
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration


