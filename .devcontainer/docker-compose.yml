version: "3.9"

services:
  app:
    image: mcr.microsoft.com/devcontainers/java:1-17-bullseye
    container_name: studyconnect-backend-app
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    environment:
      # Override datasource to use the db service hostname
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/studyconnect
      SPRING_DATASOURCE_USERNAME: studyconnect_user
      SPRING_DATASOURCE_PASSWORD: studyconnect_password
    depends_on:
      - db

  db:
    image: postgres:16
    container_name: studyconnect-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: studyconnect
      POSTGRES_USER: studyconnect_user
      POSTGRES_PASSWORD: studyconnect_password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: